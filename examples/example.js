// Write modular code!

require('./pfioInput.js'); // Watches the Piface inputs for changes
require('./pfioEchoOutput.js'); // Echoes changes to the inputs onto the outputs
require('./hold.js'); // Adds an event when an input is held for 500ms (try it with the tactile switches on the piface!)